{
  "name": "hello-world-llm",
  "version": "1.0.0",
  "description": "A hello-world level LLM built from scratch in TypeScript — no ML frameworks",
  "scripts": {
    "start": "npx tsx src/main.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.0.0"
  }
}
